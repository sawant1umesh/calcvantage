---
import PageContainer from '../components/layout/PageContainer.astro';
import Section from '../components/base/Section.astro';
import NetWorthCalculatorPanel from '../components/networth/NetWorthCalculatorPanel.astro';
import NetWorthResultsPanel from '../components/networth/NetWorthResultsPanel.astro';
import NetWorthEducationalContent from '../components/networth/NetWorthEducationalContent.astro';

interface Props {
  title?: string;
  subtitle?: string;
}

const {
  title = 'Net Worth Calculator',
  subtitle = 'Track your total assets, liabilities, and net worth instantly with detailed allocation and debt breakdown for the US, Canada, and Australia.',
} = Astro.props;
---

<PageContainer maxWidth="xl">
  <Section title={title} subtitle={subtitle} badge="CalcVantage Financial Tools" heading="h1">
    <div class="grid-networth-layout">
      <!-- Left Panel: Calculator Controls -->
      <div class="panel-left">
        <slot name="calculator-panel">
          <NetWorthCalculatorPanel />
        </slot>
      </div>

      <!-- Right Panel: Results & Summary -->
      <div class="panel-right">
        <slot name="results-panel">
          <NetWorthResultsPanel />
        </slot>
      </div>
    </div>

    <NetWorthEducationalContent />
  </Section>
</PageContainer>

<style>
  .panel-left,
  .panel-right {
    width: 100%;
  }
</style>